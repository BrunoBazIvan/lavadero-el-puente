import { Pendiente } from '@/components/Pendiente';

export default function Tablero() {
  return (
    <Pendiente
      titulo="Tablero"
      etapa={8}
      detalle="Buscador unificado, conteos por estado, lista de órdenes con filtros y el aviso de las listas sin retirar hace más de 7 días."
    />
  );
}
